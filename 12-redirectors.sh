#!/bin/bash

redirectors are 2 of types in bash :
  1) Input rediretor (<)            - we used this to take input from a file : < (Ex : sudo mysql </tmp/studentapp.sql)
  2) Output Redirector ( >> or >)( means routing the output to a file)       : > or 1> or >> or &> ( >> appends the latest output to the existing content)